export class Player {
  constructor(public name: string, public number: number, public age: number, public height: string, public posistion: string, public gp: number, public mpg: number, public points: number) {}
}
